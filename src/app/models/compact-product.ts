export interface CompactProduct {
  product_id: number;
  title: string;
  base_rate: number;
  rate: number;
  gst_amount: number;
  base_quantity: string;
  no_of_units: number;
  image_url: string;
}
